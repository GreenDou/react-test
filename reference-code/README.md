# Reference code for React 19 features

这些示例是给页面“代码案例讲解区”用的真实项目片段：
- 它们放在 `reference-code/`，表示“仓库中真实存在，但不参与当前静态 Vite 页面运行”。
- 页面会通过原始文本导入（raw import）展示它们的内容。
- 这样既能在 GitHub Pages 上讲清楚边界，也不会伪造一个根本跑不起来的在线 demo。
